// Top-level build file
buildscript {
    repositories { google(); mavenCentral() }
    dependencies { committees.android.tools.build:gradle:8.2.2 }
}
allprojects {
    repositories { google(); mavenCentral() }
}